[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["openclaw"]

[project]
name = "openclaw-python"
version = "0.8.2"
description = "Personal AI assistant platform - Python implementation of OpenClaw"
readme = "README.md"
requires-python = ">=3.11,<3.14"
license = { text = "MIT" }
authors = [
    { name = "OpenClaw Contributors" }
]
keywords = ["ai", "assistant", "chatbot", "agent", "llm", "openclaw", "personal-ai"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
]

dependencies = [
    # pi-mono-python infrastructure (path deps for workspace alignment)
    "pi-agent",
    "pi-ai",
    "pi-coding-agent",
    "pi-tui",
    # Core
    "fastapi>=0.109.0",
    "uvicorn[standard]>=0.27.0",
    "websockets>=12.0",
    "aiohttp>=3.9.0",  # For unified HTTP+WebSocket server
    "pydantic>=2.5.0",
    "pydantic-settings>=2.1.0",
    "typer[all]>=0.9.0",
    "rich>=13.7.0",
    "questionary>=2.0.0",  # Interactive CLI prompts
    "nest-asyncio>=1.6.0",  # Allow nested event loops for questionary
    # LLM (Updated: google-genai for new API)
    "anthropic>=0.18.0",
    "openai>=1.12.0",
    "google-genai>=0.2.0",  # New Gemini API
    "boto3>=1.34.0",  # AWS Bedrock
    # Channels
    "python-telegram-bot>=21.0",
    "discord.py>=2.3.0",
    "PyNaCl>=1.5.0",  # Required for Discord voice encryption
    "slack-sdk>=3.27.0",
    # Tools
    "playwright>=1.41.0",
    "httpx>=0.27.0",
    "croniter>=2.0.0",  # Cron expression parsing
    # Storage
    # "lancedb>=0.5.0",  # Temporarily disabled for macOS x86_64 compatibility
    "aiosqlite>=0.19.0",
    # "pyarrow>=14.0.0",  # Temporarily disabled (dependency of lancedb)
    # Search and Data
    "ddgs>=9.0.0",  # DuckDuckGo search (renamed from duckduckgo-search)
    # "sentence-transformers>=2.5.0",  # Temporarily disabled (requires torch)
    # "torch>=2.1.0",  # Temporarily disabled for macOS x86_64 compatibility
    # Scheduler
    "apscheduler>=3.10.0",
    # Voice
    "elevenlabs>=0.2.0",
    # Tools
    "pillow>=10.0.0",
    "psutil>=5.9.0",
    "watchdog>=6.0.0",
    "python-pptx>=0.6.23",  # PowerPoint generation
    "reportlab>=4.0.0",  # PDF generation
    # Utils
    "pyyaml>=6.0",
    "pyjson5>=1.6.0",
    "python-dotenv>=1.0.0",
    "aiofiles>=23.2.0",
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
    "pytest-cov>=7.0.0",
    "lark-oapi>=1.5.3",
    "websocket-client>=1.9.0",
]

[project.optional-dependencies]
channels = [
    "matrix-nio>=0.24.0",
    "line-bot-sdk>=3.5.0",
    "mattermostdriver>=7.3.0",
    "botbuilder-core>=4.15.0",
    "msgraph-sdk>=1.0.0",
    "google-cloud-pubsub>=2.18.0",
    "google-auth>=2.23.0",
]
feishu = [
    "lark_oapi>=1.5.3",
]
# WhatsApp channel uses Baileys bridge (Node.js subprocess).
# Python deps (aiohttp, pillow) are already in core.
# To use WhatsApp: cd extensions/whatsapp/bridge && npm install
whatsapp = []
# Discord voice requires libopus installed on the system (libopus-dev / opus)
discord = [
    "PyNaCl>=1.5.0",  # Already in core — listed here for clarity
]
voice = [
    "twilio>=8.0.0",
]
all = [
    "matrix-nio>=0.24.0",
    "line-bot-sdk>=3.5.0",
    "mattermostdriver>=7.3.0",
    "botbuilder-core>=4.15.0",
    "msgraph-sdk>=1.0.0",
    "google-cloud-pubsub>=2.18.0",
    "google-auth>=2.23.0",
    "twilio>=8.0.0",
    "lark_oapi>=1.5.3",
]

[project.scripts]
openclaw = "openclaw.cli.main:app"

[project.urls]
Homepage = "https://github.com/openclaw/openclaw-python"
Documentation = "https://github.com/openclaw/openclaw-python/tree/main/docs"
Repository = "https://github.com/openclaw/openclaw-python"
Issues = "https://github.com/openclaw/openclaw-python/issues"
"Main Project" = "https://github.com/openclaw/openclaw"

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=4.1.0",
    "pytest-mock>=3.12.0",
    "pytest-timeout>=2.4.0",
    "black>=24.0.0",
    "ruff>=0.2.0",
    "mypy>=1.8.0",
    "twine>=5.0.0",
]

[tool.hatchling.build.targets.wheel]
packages = ["openclaw"]

[tool.black]
line-length = 100
target-version = ['py311']

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP"]
ignore = [
    "E501",  # Line too long (handled by black)
    "E722",  # Bare except (some tools need it)
    "E402",  # Module level import not at top (some cases need lazy import)
]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]
"tests/**" = ["F401", "F811"]
"examples/*.py" = ["F401"]  # Examples may have unused imports
"extensions/*/plugin.py" = ["F401", "F821"]  # Extensions are stubs
"openclaw/channels/*.py" = ["F401", "F821"]  # Channels may be incomplete
"openclaw/config/schema.py" = ["N815"]  # Keep TypeScript naming
"openclaw/gateway/protocol/*.py" = ["N815"]  # Protocol uses camelCase
"openclaw/cli/main.py" = ["F401"]  # CLI checks imports for availability

[tool.mypy]
python_version = "3.11"
warn_return_any = false
warn_unused_configs = true
disallow_untyped_defs = false
ignore_missing_imports = true
no_implicit_optional = false
warn_unused_ignores = false

# Ignore specific issues in work-in-progress modules
[[tool.mypy.overrides]]
module = [
    "openclaw.channels.*",
    "openclaw.plugins.*",
    "openclaw.gateway.*",
]
ignore_errors = true
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
python_classes = "Test*"
python_functions = "test_*"
asyncio_mode = "auto"
addopts = [
    "-v",
    "--tb=short",
    "--strict-markers",
    "--cov=openclaw",
    "--cov-report=term-missing",
    "--cov-report=html",
]
markers = [
    "asyncio: mark test as async",
    "slow: mark test as slow",
    "integration: mark test as integration test",
]

[tool.coverage.run]
source = ["openclaw"]
omit = ["*/tests/*", "*/test_*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
    "@abstractmethod",
]

# ---------------------------------------------------------------------------
# pi-mono-python path dependencies (resolved by uv)
# ---------------------------------------------------------------------------
[tool.uv.sources]
pi-agent      = { path = "../pi-mono-python/packages/agent",        editable = true }
pi-ai         = { path = "../pi-mono-python/packages/ai",           editable = true }
pi-coding-agent = { path = "../pi-mono-python/packages/coding-agent", editable = true }
pi-tui        = { path = "../pi-mono-python/packages/tui",          editable = true }
