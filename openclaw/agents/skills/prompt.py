"""
Format skills for inclusion in system prompt — LEGACY / UNUSED.

The canonical implementation lives in ``loader.format_skills_for_prompt`` (accepts
``list[Skill]``) and ``system_prompt_sections.build_skills_section`` (adds the
``## Skills (mandatory)`` header).

This module is kept empty to avoid import confusion.
"""
